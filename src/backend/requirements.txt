# ── FastAPI + ASGI ──
fastapi>=0.136
uvicorn[standard]>=0.46
sse-starlette>=3.3

# ── Database ──
sqlalchemy[asyncio]>=2.0
asyncpg>=0.31
psycopg[binary]>=3.3
alembic>=1.18

# ── Auth ──
python-jose[cryptography]>=3.5
bcrypt>=5.0
python-multipart>=0.0.26

# ── Validation & Settings ──
pydantic>=2.12
pydantic-settings>=2.14

# ── HTTP & Networking ──
httpx>=0.28
# hermes-agent 0.17.0 pinnt websockets==15.0.1 (hart); untere Grenze entsprechend
# gelockert (vorher >=16.0), damit pip beide Constraints erfuellen kann. uvicorn
# [standard] & Co. laufen problemlos mit 15.x.
websockets>=15.0,<16
cachetools>=7.0

# ── Image Processing ──
Pillow>=11.0

# ── LLM & Agent ──
litellm>=1.83
openai>=2.24
anthropic>=0.97
tiktoken>=0.12
# EXAKT gepinnt (nicht >=): reproduzierbare Builds, kein unbemerkter Versions-
# sprung beim naechsten docker build. 0.17.0 bringt das atomare Memory-Batch-Tool
# (add/replace/remove gegen das finale Char-Budget in EINEM Call) -- behebt genau
# unser "Memory voll / Schreiben schlaegt mitten im Edit fehl"-Problem. Upgrade
# bewusst + getestet erhoehen (Eval-Suite), nie automatisch.
hermes-agent==0.17.0

# ── MCP (Model Context Protocol) ──
mcp>=1.27

# ── Markdown & Content ──
Markdown>=3.10

# ── Document Export & Content-Services ──
# contentconverter: privates Package (Export, Anonymisierung, Extraktion)
#   pip install -e /path/to/contentConverter[all]
# Transitive Dependencies:
python-docx>=1.2
python-pptx>=1.0
pyyaml>=6.0

# Pandoc (als Python-Package mit gebündeltem Binary — kein apt nötig)
pypandoc_binary>=1.17

# System-Dependencies für Document Export (siehe Dockerfile):
#   - libreoffice   (apt: libreoffice-writer) → PDF-Konvertierung via docx→pdf
#   - pandoc wird via pypandoc_binary mitgeliefert, Symlink in venv/bin nötig

# Anonymisierung (via contentconverter[anonymize])
# presidio-analyzer, presidio-anonymizer, faker, spacy, gliner
# werden transitiv durch contentconverter[all] installiert

# ── PDF-Extraktion (OneDrive-Downloads) ──
pymupdf>=1.27

# ── XLSX-Extraktion (Dokumenten-Kontext-Brücke) ──
openpyxl>=3.1

# ── Web Search ──
ddgs>=9.14

# ── Scheduling ──
# hermes-agent pinnt croniter==6.0.0 (PyPI-Wheel); untere Grenze entsprechend
# gelockert, damit pip beide Constraints erfüllen kann (API ist stabil).
croniter>=6.0,<7
APScheduler>=3.11

# ── Security ──
pyotp>=2.9
slowapi>=0.1.9
bleach>=6.2
aioclamd>=1.0

# ── Testing ──
pytest>=9.0
pytest-asyncio>=1.3
respx>=0.23
playwright>=1.52
pytest-playwright>=0.7
